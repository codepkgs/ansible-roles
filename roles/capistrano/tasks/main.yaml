---
- name: install capistrano packages
  gem:
    name: "{{ item }}"
    state: present
    user_install: no
  with_items:
    - capistrano
    - capistrano-scm-copy
    - net-ssh
    - bcrypt_pbkdf
    - ed25519