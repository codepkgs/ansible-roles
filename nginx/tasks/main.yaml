---
- name: install dependency packages
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - epel-release
    - pcre
    - pcre-devel
    - openssl
    - openssl-devel
    - zlib
    - zlib-devel
    - rsync

- name: copy nginx repository
  copy:
    src: nginx.repo
    dest: /etc/yum.repos.d/nginx.repo
    owner: root
    group: root
    mode: 0644

- name: install nginx
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - nginx
    - nginx-mod-stream

- name: delete nginx default config
  file:
    path: /etc/nginx/conf.d/default.conf
    state: absent
  when:
    - nginx_delete_config | bool

- name: copy nginx config
  copy:
    src: nginx.conf
    dest: /etc/nginx/nginx.conf
    owner: root
    group: root
    mode: 0644
  notify:
    - reload nginx

- name: create nginx extra directory
  file:
    path: "{{ item }}"
    owner: root
    group: root
    mode: 0755
    state: directory
  with_items:
    - /data/logs/nginx
    - /opt/scripts

- name: sync nginx certs
  synchronize:
    src: "{{ nginx_certs_dir }}"
    dest: /etc/nginx/certs
    owner: no
    group: no
    delete: yes
    recursive: yes
  notify:
    - reload nginx
  when:
    - nginx_certs_dir | bool

- name: sync nginx extra config
  synchronize:
    src: "{{ nginx_config_dir }}/"
    dest: /etc/nginx/conf.d
    owner: no
    group: no
    delete: yes
    recursive: yes
  notify:
    - reload nginx
  when:
    - nginx_certs_dir | bool

- name: ensure nginx is running
  service:
    name: nginx
    enabled: yes
    state: started

- name: copy nginx logrotate script
  copy:
    src: logrotate_nginx
    dest: /opt/scripts/logrotate_nginx
    owner: root
    group: root
    mode: 0644

- name: nginx logrotate cron
  cron:
    name: nginx logrotate
    job: "/usr/sbin/logrotate -f /opt/scripts/logrotate_nginx"
    state: present
    user: root
    hour: "23"
    minute: "59"
